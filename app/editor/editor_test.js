'use strict';

describe('wetaApp.editor module', function() {

  beforeEach(module('wetaApp.editor'));

  describe('editor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var edCtrl = $controller('EditorCtrl');
      expect(edCtrl).toBeDefined();
    }));

  });
});