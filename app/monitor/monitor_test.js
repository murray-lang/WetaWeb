'use strict';

describe('wetaApp.monitor module', function() {

  beforeEach(module('wetaApp.monitor'));

  describe('monitor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var monCtrl = $controller('MonitorCtrl');
      expect(monCtrl).toBeDefined();
    }));

  });
});