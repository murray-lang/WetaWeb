'use strict';

describe('wetaApp.blockly module', function() {

  beforeEach(module('wetaApp.blockly'));

  describe('blockly controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var blocklyCtrl = $controller('BlocklyCtrl');
      expect(blocklyCtrl).toBeDefined();
    }));

  });
});