'use strict';

describe('wetaApp.configure module', function() {

  beforeEach(module('wetaApp.configure'));

  describe('configure controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var configCtrl = $controller('ConfigCtrl');
      expect(configCtrl).toBeDefined();
    }));

  });
});